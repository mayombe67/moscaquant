# MQ-5.ER.2 — THE RACKET — Results

## Status

Confirmatory result complete.

- Experiment: `mq5-er2-the-racket-v1`
- Codename: `THE RACKET`
- Authoritative git head: `b5f9030459c167ffa12b6b7344814b9a4774e37c`
- Result artifact SHA-256: `3c5e18e63330d99c591536a59db9e6d40c5e5e8dca0789b576fa573e6e0bbfb1`
- Frames: `192`
- Financial semantics: `NOT ASSIGNED`

## Frozen primary result

The preregistered primary comparison was `C13R` versus `C13`.

Observed classification:

`RACKET_CAUSAL_SUPPORT_NOT_OBSERVED`

Primary dependency count: `0 / 3`.

Primary targets:

- `92`: no later onset and no loss
- `656`: no later onset and no loss
- `137122`: no later onset and no loss

For every measured target, `C13R` reproduced the same first-positive onset as
`C13`.

## Matched-control result

The preregistered matched-control comparison was `C13C` versus `C13`.

Observed calibration:

`NO_PRIMARY_DEPENDENCY_IN_EITHER_INTERVENTION`

The structurally matched control edge `78481 -> 16087` produced no primary
dependency signal.

## Secondary descriptive result

All four intervention arms had normalized responder fingerprint distance `0.0`
from their respective reference condition:

- `CR` versus `C0`: `0.0`
- `C13R` versus `C13`: `0.0`
- `CC` versus `C0`: `0.0`
- `C13C` versus `C13`: `0.0`

Target-set specificity:

`SPECIFICITY_NOT_ESTABLISHED`

## Post-result integrity audit

A post-result audit found that both the focused DETOUR edge and the matched
control are retina-to-relay edges:

- focused edge `116680 -> 12024`
- matched control `78481 -> 16087`

For both edges:

- presynaptic neuron is in the frozen retina population;
- postsynaptic neuron is in the frozen relay population.

The runtime computes ordinary connectome synaptic current and subsequently
subtracts frozen direct retinal current at relay neurons as part of retinal
double-count removal.

Therefore DETOUR's discovery score could rank a pre-cancellation ordinary
synaptic contribution that is not a net effective contribution to the runtime
state.

This does not alter THE RACKET's frozen confirmatory result. It changes the
interpretation of why the nominated edge had no detectable causal effect.

## Scientific interpretation

THE RACKET does **not** support necessity of `116680 -> 12024` for the tested
Arm-C responder behavior.

The result does not establish sufficiency, uniqueness, biological causality,
cognition, financial semantics, market prediction, or trading relevance.

The post-result audit identifies a methodological issue in DETOUR candidate
eligibility/scoring: future discovery must either exclude retina-to-relay
ordinary-connectome contributions that are canceled by frozen retinal
double-count removal, or score net effective runtime contribution after that
cancellation.

## Lore

DETOUR found the same guy in three witness statements. THE RACKET pulled him
out of the operation and nothing changed.

**THE RACKET got Keyser Söze'd.**

`116680 -> 12024` is now canonically **THE NO-SHOW**: he was on the books, looked
important in the accounting, and turned out not to be doing the job DETOUR
thought he was doing.

The matched-control civilian was also a retina-to-relay no-show, which explains
why neither member of the lineup changed the operation.
